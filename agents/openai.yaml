interface:
  display_name: "Wallabag"
  short_description: "Wallabag CRUD via OAuth env credentials"
  default_prompt: "Use $wallabag to manage Wallabag bookmarks; required WALLABAG_* env vars must be set first."
